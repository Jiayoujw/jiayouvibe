name: Deploy to Namecheap

on:
  push:
    branches: [main, master]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Verify build
        run: |
          grep -c './' dist/index.html && echo "✅ Relative paths" || echo "❌ Absolute paths"
          head -2 dist/index.html

      - name: Install lftp
        run: sudo apt-get update -qq && sudo apt-get install -y -qq lftp

      - name: Deploy to Namecheap
        run: |
          lftp -e "
            set ftp:ssl-allow no;
            set ssl:verify-certificate no;
            open ${{ secrets.FTP_SERVER }};
            user ${{ secrets.FTP_USERNAME }} ${{ secrets.FTP_PASSWORD }};

            # Upload to BOTH possible web roots
            mirror -R -v --delete ./dist/ ./;
            mkdir -p ./public_html;
            mirror -R -v --delete ./dist/ ./public_html/;

            # Also try common subdirectory patterns
            mkdir -p ./public_html/jiayouvibe.com;
            mirror -R -v --delete ./dist/ ./public_html/jiayouvibe.com/;

            echo '=== Root listing ===';
            ls;
            echo '=== public_html listing ===';
            ls public_html/;
            echo '=== Root index.html ===';
            head -2 index.html;
            bye;
          "

      - name: Purge Cloudflare cache
        run: |
          ZONE_ID=$(curl -s -X GET "https://api.cloudflare.com/client/v4/zones?name=jiayouvibe.com" \
            -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
            -H "Content-Type: application/json" | jq -r '.result[0].id')
          echo "Zone ID: $ZONE_ID"
          if [ -n "$ZONE_ID" ] && [ "$ZONE_ID" != "null" ]; then
            curl -s -X POST "https://api.cloudflare.com/client/v4/zones/$ZONE_ID/purge_cache" \
              -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
              -H "Content-Type: application/json" \
              --data '{"purge_everything":true}'
          fi
